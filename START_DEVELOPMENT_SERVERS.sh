# These commands will start the development servers for the project.
# Both the React development server and the docker container must be running for the project to work.
# From the root directory of this project, start the frontend server container:
docker compose up --detach
# Start the React development server:
cd frontend
npm run dev
