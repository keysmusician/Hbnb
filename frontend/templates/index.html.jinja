<!DOCTYPE html>
<html lang="en">

<head>
	{% include 'head.html.jinja' %}
	<link rel="stylesheet" type="text/css" href="../static/styles/filters.css?{{ cache_id }}">
	<link rel="stylesheet" type="text/css" href="../static/styles/places.css?{{ cache_id }}">
</head>

<body>
	<div id="filter_dialog"></div>
	{% include 'header.html.jinja' %}
	<main class="container">
		<section id="filters" title="Filters"></section>
		<section id="places">
			{% for place in places %}
			<a href="/places/{{ place.id }}">
				<article>
					<div class="title_box">
						<h2>{{ place.name }}</h2>
					</div>
					<div class="city">
						<b>{{ place.city.name }}, {{ place.city.state.name }}</b>
					</div>
					<div class="information">
						<div class="max_guest">{{ place.max_guest }} Guest{% if place.max_guest != 1 %}s{% endif %}</div>
						<div class="number_rooms">{{ place.number_rooms }} Bedroom{% if place.number_rooms != 1 %}s{% endif %}</div>
						<div class="number_bathrooms">{{ place.number_bathrooms }} Bathroom{% if place.number_bathrooms != 1%}s{% endif %}</div>
					</div>
					<div class="description">
						{{ place.description | safe }}
					</div>
					<div class="price_by_night">
						<b>${{ place.price_by_night }}</b> night
					</div>
				</article>
			</a>
			{% endfor %}
		</section>
	</main>
	{% include 'footer.html.jinja' %}
</body>

</html>
